# Set the SH_KIND environment variable to reflect the shell being run.
# Note: DO NOT EXPORT THIS VALUE!!! If you run a different kind of subshell,
# let it determine the value on its own, rather than inherit the wrong value.

SH_KIND=bash
